create a virtualenv 
cmd py -m venv .venv
intall requirements pip install flask opencv-python mediapipe numpy scikit-learn
activate env .\.venv\Scripts\activate
py to check versions {
pip install scikit-learn==1.3.3
Python 3.10.x
 mediapipe==0.10.9 
 pip install numpy==1.26.4
}
run  python live_recognition.py   //to run detection code
run python train_sign.py// to trainsignlng
